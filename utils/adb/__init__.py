"""Cohesive submodules extracted from the ``utils.adb_tools`` god-file (#63).

``utils.adb_tools`` re-exports these so existing imports keep working while the
implementation is split into focused, testable modules.
"""
